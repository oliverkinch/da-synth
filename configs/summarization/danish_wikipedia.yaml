task: summarization
seed_dataset: oliverkinch/danish_wikipedia
seed_subset: default
seed_split: train
text_template: "# {title}\n\n{text}"
n_samples: 5000
max_seed_chars: 4000
filters:
  min_assistant_tokens: 30
  max_repetition_ratio: 0.35
  language_check: true
